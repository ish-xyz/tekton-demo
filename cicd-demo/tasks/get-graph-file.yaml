apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: get-graph-file
  namespace: tekton-demo
spec:
  params:
  - name: app-name
    type: string
  resources:
    inputs:
    - name: git-graph-files
      type: git
  results:
  - name: current-version
    description: The current date in unix timestamp format
  - name: candidate-version
    description: The current date in human readable format
  steps:
  - name: store-candidate-version-number
    image: bash:latest
    script: |
      #!/usr/bin/env bash
      source $(resources.inputs.git-graph-files.path)/graph-files/$(params.app-name)/graph.source
      echo "$(( $DOCKER_IMAGE_VERSION + 1 ))" | tee /tekton/results/candidate-version

  - name: store-current-version-number
    image: bash:latest
    script: |
      #!/usr/bin/env bash
      source $(resources.inputs.git-graph-files.path)/graph-files/$(params.app-name)/graph.source
      echo $DOCKER_IMAGE_VERSION | tee /tekton/results/current-version
